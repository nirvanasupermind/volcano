var t1 = ThreadUtils.makeThread(afunc () {
    for(var i = 0; i < 10; i++) {
        print("Thread ");
        println(ThisThread.getID());
        println(i);
        ThisThread.sleepFor(1000);
    }
});

var t2 = ThreadUtils.makeThread(afunc () {
    for(var i = 0; i < 10; i++) {
        print("Thread ");
        println(ThisThread.getID());
        println(i);
        ThisThread.sleepFor(990);
    }
});


ThreadUtils.join(t1);
ThreadUtils.join(t2);