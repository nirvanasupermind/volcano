var t1 = ThreadUtils.makeThread(afunc () {
    for(var i = 0; i < 10; i++) {
        print("Thread 1: ");
        println(i);
    }
});

var t2 = ThreadUtils.makeThread(afunc () {
    for(var i = 0; i < 10; i++) {
        print("Thread 2: ");
        println(i);
    }
});


ThreadUtils.join(t1);
ThreadUtils.join(t2);