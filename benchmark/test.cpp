
#include <iostream>
#include <map>
#include <string>
#include <vector>
#include <functional>
#include <sstream>
#include <random>
#include "/usr/local/include/tachyon_stl.h"
    
int main(){
tachyon_stl_setup();
    auto start = std::chrono::system_clock::now();

for(uint64_t i = 13633365291550375937ULL;((unpack_number(i)<unpack_number(13633366121223707137ULL) ? 10ULL : 2ULL)) != 2ULL;i = pack_number(unpack_number(i) + 1.0f)){
uint64_t x = 9453958458664550401ULL;
}

    auto end = std::chrono::system_clock::now();
    std::chrono::duration<double> elapsed_seconds = end - start;
    std::cout << "elapsed time: " << elapsed_seconds.count() << "s\n";


free_all_objects();
return 0;
}